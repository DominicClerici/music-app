# Caveats — Deviations from the Pinned Design

The design docs (`ROADMAP.md`, `PHASE_1.md`–`PHASE_8.md`) are binding, but implementation will occasionally force a deviation. Every deviation is logged here so its impact stays visible to later sessions, which otherwise have no memory of it.

**Log an entry when:** a pinned contract/algorithm/data format is changed or reinterpreted; a printed worked-example sample is found wrong (golden-value arbitration rule 2, `ROADMAP.md` §3); an algorithm ambiguity is resolved; scope is moved between phases/sessions; a dependency pin forces a design adjustment.

**Not caveats:** bugs fixed in this project's own new code, refactors that don't change pinned contracts, or decisions the PHASE docs explicitly left open (those are just decisions — record them in the session plan and PROGRESS.md).

## Entry format

```
### C-<nn>: <one-line title>
- **Date / session:** YYYY-MM-DD, session NN (Phase N)
- **What deviated:** the pinned text vs. what was actually built
- **Why:** the concrete forcing reason
- **Impact:** downstream PHASE docs / phases / fixtures affected; what a later session must know
- **Doc amendment:** commit hash of the PHASE/ROADMAP amendment, or "none needed" with justification
- **Status:** open | resolved
```

Numbered sequentially (`C-01`, `C-02`, …), never renumbered. If a later session resolves or supersedes an entry, update its **Status** in place and note how — do not delete entries.

## Log

### C-24: PHASE_8 §5.4's "triplet-grid snare/tom fill" is unrealizable under W7 — fill tiling mixes grids inside one snare Phrase; `bl_dr_f1` re-authored on the straight grid
- **Date / session:** 2026-07-21, session 21 (Phase 8, chunk 7), T5 integration
- **What deviated:** §5.4 pins the blues fill convention as "Fills: triplet-grid snare/tom figures; rung-4 tom runs." Authored that way, W7 (grid homogeneity per (section, track) Phrase on stage-6 output, `quality/layer1.py` — itself pinned by §8.1/§3.1) fired on **190 of 192** rendered cells: fill selection is grid-blind, so a triplet-grid snare fill tiles into snare Phrases that also carry straight-grid snare content (main-pattern backbeats, the tom-run fill's pos-240 accent, rung-4 gated ghosts), mixing grids inside one Phrase. Resolution: `bl_dr_f1` re-authored as a straight-grid snare buildup (pos 960–1800, monotone 0.40→0.76); no snare pattern is now triplet-grid, and the full 192-cell sweep is W7-clean. The 12/8 triplet feel still lives on the ride/kick of the gated `bl_dr_3s`/`bl_dr_3sb` mains.
- **Why:** two pinned rules conflict, and only one reading renders valid — the C-05 pattern (the normative rule wins over the prose sample). §3.1's one-grid rule was written per *pattern*; W7 enforces it per *Phrase*, and fills compose into Phrases with main-pattern content of the other grid. No fill authoring can satisfy both while any straight-grid snare content exists at the same boundary.
- **Impact:** `styles/blues/patterns/drums.yaml` (`bl_dr_f1`). Generalizes: **a triplet-grid fill is only safe if every pattern that can share its (section, track) Phrase is also triplet-grid** — future packs (fusion C8) must author fills on the grid their role's mains use. **Lint gap (adjacent to C-23's):** `packs/lint.py` grid-mixing checks per-pattern purity only, never cross-pattern Phrase-level composition — a loader-time warning cannot catch this; candidate C9 lint improvement. §5.4's fills wording fix is **flagged for user sign-off** (raise at the T8 gate / close-out); the content + tests land now per arbitration rule 2's same-commit discipline for the forced part.
- **Doc amendment:** pending sign-off (one line: blues fills author on the straight grid; triplet fills require an all-triplet role context).
- **Status:** open

### C-23: blues rungs 1–2 are unreachable grid-wide (all-solo form × R2 solo arch) — §5.4's printed rung ladder re-mapped onto rungs 3/4 (S21-2, user-ratified)
- **Date / session:** 2026-07-21, session 21 (Phase 8, chunk 7), scoping → S21-2 ruling at the approval gate
- **What deviated:** §5.4 printed the drum ladder as rung 1 = sparse shuffle + tempo-gated slow-blues 12/8, rung 2 = Chicago, rung 3 = Texas, rung 4 = double-shuffle (bass/comping analogous), with "the hat→ride switch is the rung 2→3 content change." Measured with the real engine at scoping: `main`-kind patterns render **only in `solo` sections** (`parts/selection.py` — intro→intro-kind, outro→ending-kind, rung matching applies to main only), and the blues form is all-solo; the R2 solo arch (base 0.60 + 0.30·index/total, +0.10·arousal, envelope [0.15, 0.95]) puts every solo at energy **≥ 0.624** — rung 3 minimum, final solo rung 4 for all 8 moods (0.834–0.934). Rung-1/2 mains never render; the [50, 75] tempo gate would never gate a rendered pattern. **User ruled S21-2 (2026-07-21): re-map** — Chicago (`bl_dr_2` verbatim, energyLevel 2→3) + gated slow-blues 12/8 at rung 3; Texas + double-shuffle at rung 4 (the hat→ride lever moves to the 3→4 boundary, which the rising arch crosses mid-jam); rungs 1–2 authored as sparser completeness variants (PT5 + variety-lint required).
- **Why:** structural, not a bug — the C-22 mechanism in a second, stronger form (chill_lofi lost its *top* rungs to an arousal ceiling; blues loses its *bottom* rungs to a section-kind floor). The engine faithfully implements the pinned energy/selection rules; §5.4's rung expectations were derived samples that don't survive them. Accept-as-printed would have rendered Texas/double-shuffle at the melancholic 68 BPM slow tier — failing DoD §14.10's own "shuffle locks at three tempo tiers" clause.
- **Impact:** `styles/blues/patterns/*` rung-1/2 mains are golden-blind completeness content (the C-20 class; selection-locked by `tests/test_blues_variety.py`, never rendered). **Lint gap, second instance:** `packs/lint.py::_reachable_rungs` reads the envelope only (`[0.15, 0.95]` → all four rungs "reachable") and models neither arousal scaling (C-22) nor the section-kind/energy-base floor (this entry) — a per-mood/per-section-kind reachability computation is now twice-motivated for C9. No `expected-unreachable` markers are used (the lint fires no warning to silence). Blind set measured precisely at T10.
- **Doc amendment:** applied in the landing commit — §5.4 bracketed S21-2 note (measured dormancy + the re-map), per arbitration rule 2.
- **Status:** open (accepted re-map; revisit only with a signed-off form/energy design change)

### C-22: chill_lofi rung 3 is unreachable and the pads role never sounds — §4.2/§10's pad-reachability prose was wrong; dormancy accepted (flat-rung-2 is the genre)
- **Date / session:** 2026-07-20, session 20 (Phase 8, chunk 6), T6 full-grid verification
- **What deviated:** PHASE_8 §4.2 stated only rung 4 is unreachable, and §10 claimed "pads are reserved for higher-arousal moods (happy/dreamy … with rung-3 sections) or the `study` ensemble's fuller mixes." Measured over a 1200-render sweep (8 moods × 30 seeds × 5 lengths): max section energy is **0.474** (happy) — below the engine's 0.55 rung-3 threshold — so **rung 3 is unreachable grid-wide**, the rung-3 banks (16th-hat/shaker ornaments, offbeat-stab comping) are dormant, and **the pads role (8 entries + `tape_strings`/`warm_wash` timbres) never activates in any supported render**. Ensembles swap flavors, never layer counts, so `study` does not help.
- **Why:** structural, not a bug — chill_lofi deliberately drops `energetic` (D8), capping arousal at happy's +0.40; the engine faithfully implements the pinned energy/intensity formulas (§10's arithmetic verified exact, 12/12). **User ruled 2026-07-20 (S20-5): accept the dormancy** — the flat-rung-2 dramaturgy is §10's own stated genre intent; raising the §4.2 `energyRange` ceiling to force rung 3 would amend a pinned value against the pack's character.
- **Impact:** `styles/chill_lofi/patterns/*` rung-3/4 entries and `styles/chill_lofi/timbres.yaml` pad flavors are completeness/blind content (variety-lint-required; selection-locked by `tests/test_chill_lofi_variety.py`, never rendered — the C-20 golden-blind class). The C-04 precedent (jazz quartal pads dormant in v1) applies. A future pass wanting audible lo-fi pads must either widen the mood set (energetic is anti-genre), raise the envelope ceiling (§4.2 amendment + recomputed §10 ladder + recalibration), or change engine layer-count rules — all sign-off matters. **Lint gap (secondary):** `packs/lint.py` reachability reads the envelope ceiling (0.60 → rung 3 "reachable") without modeling arousal scaling, so no unreachable-content warning fires for rung 3; a future lint pass could compute the true per-mood max energy. Calibration bands (T7) encode the pads-silent state — recalibrate if this ever changes.
- **Doc amendment:** applied 2026-07-20 in the T7 commit — PHASE_8 §4.2 note + §10 arrangement bullet annotated with the measured truth (S20-5 sign-off recorded inline).
- **Status:** open (accepted dormancy; revisit only with a signed-off design change)
- **Postscript (2026-07-21, T10 lens C measurement):** the full blind set is broader than the rung-3/pads prose above — a 512-render sweep shows **34 of 50 chill_lofi pattern ids never render**: all rung-1 AND rung-3/4 mains, the entire pads role, both fills (`lf_dr_f1/f2`), and comping intro. Only 16 ids render (rung-2 mains + drum/bass intro/ending + comping ending). This is the C-20 blind class at its chill_lofi extent: blind entries have selection-lock coverage only (win-the-draw), no render-path validity coverage — a loader-valid blind entry that would fail Layer 1 if ever routed passes today's suite. Candidate remedy (lens C's G-1, not built): a dormant-content dry-render test force-routing each blind entry through retarget→voicing→serialize and asserting `validate_document == []`; weigh at C9 alongside C-20's corpus-growth decision.

### C-21: `retarget.registerLow`/`registerHigh` are inert for `degree: chord` events — voicing registers are bounded only by the arrangement lane, so the authored comping window cannot prevent L2-2 bass/comping crossings
- **Date / session:** 2026-07-20, session 19 (Phase 8, chunk 5), T2 + independent review confirmation
- **What deviated:** PHASE_5 §3.3 presents the pattern-level `retarget` register window (`registerLow`/`registerHigh`) as the authored register control for retargeted content. In the implementation, that window is consulted only on the single-degree path (`parts/retarget.py::_place_degree` → `_anchor`); `degree: chord` events return `voicing_for(chord_event)` directly (`_emit_segment`), whose pitches come from the Viterbi voicing pass (`parts/voicing.py::build_voicing_map`) pruned solely against the role's bias-shifted **arrangement lane** (`theory/voicing.py::voicing_candidates`). Jazz comping authors only `degree: chord` events with `registerLow: 52`, yet voicings legitimately reach the lane floor (46–50 by mood) — below the authored window — producing the L2-2 co-attack crossings C4 measured (42/915 cells; re-measured at 72 crossings across 400 cells in session 19). No pack-data edit can change this: raising `registerLow` has zero effect on chord-degree voicings.
- **Why:** logged, not fixed (user-ratified 2026-07-20, decision S19-5 "accept + caveat"): closing it means an engine change — intersecting the voicing candidate lane with the pattern's authored retarget window — which is outside C5's approved scope, would further diverge goldens mid-chunk, and needs its own design pass (the intersection could over-constrain the DP's candidate set; W1 lane compliance and voicing-golden interplay must be re-verified). L2-2 is warn-only/non-gating (C-16), so the crossings are a quality signal, not a defect gate.
- **Impact:** `styles/jazz/patterns/comping.yaml`'s `retarget.registerLow/High` are effectively documentation-only for its all-chord bank (true of any pack authoring chord-degree comping/pads). Pack authors (C6–C8!) must know the arrangement lane — not the pattern window — is the real register control for chord voicings; a new pack wanting a higher comping floor must get it from lane/`registerBias` data or accept the warn. The L2-2 jazz crossing signal stays open as a warn. Candidate engine remedy for a future pass: lane ∩ retarget-window pruning in `build_voicing_map`, gated by re-verifying PHASE_5 §9.3 voicing goldens and W1.
- **Doc amendment:** none applied — §3.3's window semantics for the single-degree path are implemented as pinned; the chord-path gap is an unspecified integration point (the C-04/C-14 pattern). Flagged for a future PHASE_5 §3.3/§6.4 clarification ("chord-degree events take registers from the arrangement lane; the retarget window governs single-degree placement only").
- **Status:** open

### C-20: PHASE_8 §8.2's pinned corpus matrix never selects 18 of the 49 reference patterns (37 %) — every rung-1 pattern, both fills, and nearly all pads
- **Date / session:** 2026-07-20, session 18 (Phase 8, chunk 4), T5 deliberate-change rehearsal
- **What deviated:** §8.2 positions the golden corpus as the regression surface for pack data ("a golden diff therefore localizes to the first divergent stage"). Measured during the rehearsal: the 24-cell corpus selects only **31 of the two reference packs' 49 patterns**. The 18 unreached are every **rung-1** pattern (`pr_dr_1`, `pr_bs_1`, `pr_cp_1`, `pr_pd_1`, `jz_dr_1`, `jz_cp_1`, `jz_pd_1`, …), **both fills** (`pr_dr_f1`/`pr_dr_f2`, `jz_dr_f1`), and nearly all **pads**. A pack-data edit to any of them produces **no bless divergence at all** — the corpus is silent, not merely quiet. Found empirically: the rehearsal's first attempt edited `pr_dr_1`'s snare velocity and `trackgen bless` reported "24 cell(s), no divergence".
- **Why:** it falls out of the **pinned matrix itself**, not out of the 2-of-5-pack staging (C-17). §8.2 pins the mood dimension as "default + the supported set's V/A extremes", resolved at S18-3 to the max-(V,A)-distance pair; that triple biases every cell toward mid/high-energy sections, so the low-energy rung-1 bank is never drawn. Pads are dormant at the reference packs' `layersMax: 3` (the same dormancy C-04 records for quartal voicings). Fills are boundary devices the drawn forms did not reach at these seeds. **Adding the three new packs does not fix this** — each new pack carries the same structural blind spot, since the cause is the mood triple and the layer cap, not the pack count.
- **Impact:** bounds what DoD §14.5's artifact can catch, for every session that relies on it. **C5 in particular:** it enumerates the abridged pop_rock/jazz banks and re-blesses the corpus — any rung-1/fill/pad entry it authors or edits lands **unverified by the goldens**, and needs coverage from the C2 validator suite, the C3 audition loop, or listening instead. *(Session-19 postscript: C5's variety tests give every golden-blind second candidate SELECTION coverage — a locked seed under which it wins the production draw — but no render routes those slots, so their Layer-1/L2 VALIDITY rests on authoring review and lint only; a blind candidate that would fail validators if ever routed would pass today's suite. Session-19 measurement also narrowed the blind set: pads are NOT uniformly dormant — pop_rock aggressive-class moods route pads at rungs 3/4, and jazz pads sound in happy/energetic — so the truly-unreached set is rung-1 mains, both fills, pop comping intro, and the low-rung/intro/ending pad slots.)* Two candidate remedies, both design changes needing sign-off: add a low-energy 4th mood to the corpus triple (amends §8.2's mood clause), or pair the corpus against `trackgen lint`'s variety-coverage warnings (pop 23 / jazz 15 today) so unreached patterns are at least *named* rather than silently unregressed. Adjacent to C-02 (a pinned mechanism proven unreachable under other pinned rules).
- **Doc amendment:** none applied — §8.2's matrix is implemented exactly as pinned; what is newly known is that the pinned matrix under-covers the pattern banks it is meant to regress. Flagged for a §8.2 / §12 Q5 decision ("golden-corpus growth policy beyond 60") at C9, which this measurement directly informs.
- **Status:** open

### C-19: the 480 s smoke bucket is unreachable for pop_rock — form length is bounded by template repeat counts, not by `maxLengthSec`
- **Date / session:** 2026-07-20, session 18 (Phase 8, chunk 4), T4 smoke matrix + T6 review
- **What deviated:** PHASE_8 §8.2 pins the smoke matrix at "3 length buckets (60/180/480 s)", the 480 s bucket being never-before-rendered territory (all prior phase matrices top out at 240 s). The bucket renders and gates clean, but for **pop_rock it is musically degenerate**: `maxLengthSec: 480` yields a max rendered length of **356.57 s** across the 105-cell bucket (min 92.31 s), with a hard **104-bar** ceiling uniform across all 11 moods, because the §3 form fitter is bounded above by each template's authored repeat counts rather than by the budget. **jazz reaches 480.00 s exactly, at every one of the 5 smoke seeds** (bars to 296 vs a 108 ceiling at 180 s). Across all 105 matched pairs: 85 grew, 20 stayed equal, 0 shrank.
- **Why:** logged, not fixed — closing it means changing pinned pack data (raising pop_rock's `count.max` repeat bounds in its `forms.yaml`), a §7 reference-pack refinement decision belonging to C5 and needing sign-off, not a C4 test-authoring matter. The behavior itself is correct: the fitter honors the authored ceiling, and `maxLengthSec` is a maximum, not a target. This is the **C-02 pattern** — a pinned mechanism proven unreachable under other pinned rules.
- **Impact:** `tests/test_smoke_matrix.py` (the 105 cells at `length == 480`); pop_rock's `forms.yaml` repeat bounds are the lever. C4 added `test_long_bucket_expands_form` (matched 180 s-vs-480 s pairs, relative) and `test_long_bucket_reaches_pack_ceiling` (per-pack absolute floors `pop_rock 340.0` / `jazz 470.0`) so a silent length cap fails loudly — the per-pack floor deliberately encodes pop_rock's real ceiling and **must not** be raised to 480. C5 should decide whether pop_rock's templates should reach 8-minute forms at all (a genre question — a 480 s pop_rock track is arguably out of style) and either raise the bounds or accept and record that pop_rock's usable length ceiling is ~6 minutes. C6–C8 inherit the same question at authoring time.
- **Doc amendment:** none applied — §8.2's bucket list is unchanged and every bucket is exercised; what is newly known is that one pack cannot fill the top bucket. Flagged for a §8.2 note if C5 accepts the ceiling rather than raising it.
- **Resolution (2026-07-20, session 19, decision S19-3, user-ratified at the approval gate):** the ceiling is **accepted**. pop_rock's usable length ceiling is ~6 minutes (356.57 s at the 104-bar template ceiling) — an 8-minute pop_rock form is out of genre, so `forms.yaml` repeat bounds stay as authored (`verse_chorus_bridge`/`verse_chorus` `count.max: 3`, `chorus_first` fixed 2). The smoke-matrix per-pack floor (`pop_rock 340.0`) remains the encoded truth and must not be raised to 480. The §8.2 doc note remains flagged for C9's amendment audit (a one-line "pop_rock cannot fill the 480 s bucket by design" annotation).
- **Status:** resolved (accepted ceiling; §8.2 annotation deferred to C9)

### C-18: DoD §14.6's "smoke matrix **in CI**" is satisfied as a pytest module — the repo has no CI substrate
- **Date / session:** 2026-07-20, session 18 (Phase 8, chunk 4), decision S18-1 (user-ratified at the approval gate)
- **What deviated:** DoD §14.6 pins "**Smoke matrix** in CI (packs × moods × 3 lengths × 5 seeds, Layers 1–2)", and §8.2 pins the 300-seed sweep as running "periodically (nightly/weekly)". Both ship instead as `tests/test_smoke_matrix.py` — 315 smoke cells + 600 sweep cells (939 collected tests) inside the ordinary four-gate suite. **Nothing runs on push, on PR, or on a schedule.**
- **Why:** the repository contains **no CI substrate whatsoever** — verified independently twice at session 18: no `.github/`, `Makefile`, `tox.ini`, `.pre-commit-config.yaml`, `scripts/`, `.gitlab-ci.yml`, `.circleci/`, `noxfile.py`, or `Justfile`; `pyproject.toml` configures only `testpaths`. Choosing a CI provider is unpinned infrastructure and outside the C4 scope the user approved. The matrix's *content* is fully pinned-compliant; only its venue is not.
- **Impact:** `tests/test_smoke_matrix.py`. The gate runs on every local `uv run pytest -n auto` (~6 s of the ~52 s suite), which is stricter than nightly for the developer but provides **no** enforcement on a shared branch. The 300-seed sweep runs inline on every invocation rather than at §8.2's periodic cadence. **DoD §14.6 is NOT met as written and must not be recorded as PROVEN.** Whoever adds CI: the payload is this one module; no new tests are needed, only a workflow running the existing four gates. The Phase-8 close-out (C9) should either add the substrate or carry this forward as an explicitly signed-off gap.
- **Doc amendment:** none applied — §14.6's clause stands as pinned and unmet, deliberately, rather than being reworded to match what was built.
- **Status:** open

### C-17: C4's golden corpus covers 2 of the 5 pinned packs (24 of 60 cells); the five-pack matrix fills out in C6–C8
- **Date / session:** 2026-07-20, session 18 (Phase 8, chunk 4)
- **What deviated:** PHASE_8 §8.2 pins the corpus as "5 packs × 3 moods × 2 lengths × 2 seeds = 60 tracks", and DoD §14.5 requires "the 60-track matrix captured at every IR boundary". C4 captured **24 cells** — the two *reference* packs (pop_rock, jazz) only. All 10 IR boundaries are captured per cell at the pinned path shape; only the pack dimension is short.
- **Why:** chill_lofi/blues/fusion_jazz do not exist until C6–C8. §14's preamble pins the hard ordering ("the tooling and reference-pack items gate the new-pack items") and D13 pins tooling-before-content, so building the bless machinery before the content it will regress is the sequence the design mandates. Capturing 24 cells now is the only way to exercise the workflow at all before C5's re-bless.
- **Impact:** `fixtures/goldens/**`, `src/trackgen/tooling/corpus.py` (`_CORPUS_PACKS`). The fill-out is **purely additive** — cell coordinates are a strict prefix of the 60-cell matrix, so each of C6/C7/C8 appends its pack's 12 cells by extending `_CORPUS_PACKS` and running `trackgen bless --approve`; no existing cell moves. On-disk cost is **17 MB at 24 cells**, so expect ~42 MB at 60 (the session plan's ~12/~30 MiB estimate ran ~40 % low). **DoD §14.5 must not be marked PROVEN until C8 lands the fifth pack.** C5 will invalidate every C4 baseline (it enumerates the abridged reference banks) — expected, and the workflow's first production use. Note C-20 separately bounds what the matrix can catch *at any pack count*.
- **Doc amendment:** none needed — the pinned matrix is unchanged and unreduced; only its capture is staged across sessions per §14's own ordering.
- **Status:** open (closes when C8 captures the 60th cell)

### C-16: L2-2 voice crossing is measured at the co-attack grain (shared onsets), not §8.1's literal "sound simultaneously" (sustain overlap)
- **Date / session:** 2026-07-18, session 16 (Phase 8, chunk 2), T3 Layer-2 + whole-chunk review
- **What deviated:** PHASE_8 §8.1 pins L2-2 as "voice crossing (warn): whenever bass and comping **sound simultaneously**, `max(bass) < min(comping)`." "Sound simultaneously" reads two ways — *sustain-overlap* (any tick both voices ring) or *co-attack* (ticks where both are struck together). `quality/layer2.py::_check_l2_2_voice_crossing` implements the **co-attack** grain: crossing is checked only at shared onsets (`bass_onsets ∩ comping_onsets`), `max(sounding bass) >= min(sounding comping)` → one warn per crossing tick.
- **Why:** the literal sustain-overlap reading fires **44 warnings on the jazz reference** — a walking-bass quarter-note sustaining under independently-timed comping stabs — with **zero** crossings at any shared attack. Those are the normal rhythmic independence of a walking bass vs a comping part, not crossed voicings; a warn that always fires 44× on correct jazz is noise, not signal. Voice crossing is properly a property of a *voiced sonority* (notes struck together). **User ratified the co-attack grain 2026-07-18** (chose it over restoring the literal sweep behind the warn/fail split). This is an algorithm-text-ambiguity resolution (arbitration rule 1), not a wrong printed sample.
- **Impact:** `src/trackgen/quality/layer2.py`. L2-2 is **warn-only / non-gating** (surfaced by `suite.pipeline_warnings`, never by `validate_pipeline`), so the grain affects only a soft authoring signal. **Known limitation** (documented in the code): a bass note rising at/above a *purely-sustained* comping chord with **no shared onset** is not caught by the co-attack grain. A future pass could restore the full sustain-overlap sweep now that the warn/fail split exists (the split is what made the literal reading viable), gated by calibration signal-quality (§8.1 Q4). Both reference packs are clean under co-attack.
- **Doc amendment:** none applied — §8.1's "sound simultaneously" is ambiguous prose, resolved to the co-attack reading by user sign-off; the behavior is the binding data. Flagged for a future §8.1 L2-2 wording clarification ("at shared onsets"). Adjacent to C-08/C-15 (prose-imprecision-resolved-to-normative-behavior).
- **Status:** resolved (user-ratified interpretation; doc-wording clarification deferred)

### C-15: PHASE_7 §5.2's `envelope.*` expansion prose is self-contradictory; `allowlist.yaml` resolves it to the concrete emission set (ADSR + attackCurve)
- **Date / session:** 2026-07-18, session 14 (Phase 7, chunk 2), whole-phase contract review
- **What deviated:** PHASE_7 §5.2's parenthetical states "`envelope.*` expands to the five envelope fields + `attackCurve`" — self-contradictory, since an ADSR envelope has four fields (`attack`/`decay`/`sustain`/`release`), so "the five envelope fields + attackCurve" would be six paths. `sound/allowlist.yaml` (+ `allowlist.py:11-16`) resolves `envelope.*` to the concrete **five**-path set `attack`/`decay`/`sustain`/`release`/`attackCurve` (ADSR + attackCurve) for the subtractive/FM classes, which is exactly what the §8 recipes emit (e.g. the pop kick's `envelope.attackCurve: exponential`); MetalSynth's narrower `envelope.attack`/`decay`/`release` matches §5.2's explicit 3-field list for that class. The concrete emission set is authoritative.
- **Why:** golden-value arbitration in spirit — the §5.2 prose is imprecise (a doc wording bug), and the allowlist must cover exactly the paths the §5.1/§8 content actually emits or TB3/TB7 would false-reject the reference packs (DoD 1 "both reference files load clean"). The resolution is the only reading that both loads the pinned content clean and stays a closed allowlist (D12, no wildcards).
- **Impact:** `src/trackgen/sound/allowlist.yaml` / `allowlist.py`. No behavior risk — the reference packs load clean and the property matrix (344 docs) validates every emitted path. A Phase-8 pack (or a future §5.2 pass) adding a new envelope-bearing class must expand `envelope.*` to the same concrete set. Adjacent to C-08 (a §5.2 ride-band prose imprecision resolved to the normative data).
- **Doc amendment:** none applied — §5.2's expansion is descriptive prose and the concrete allowlist is the binding data; the resolution is documented in `allowlist.py`'s module docstring. Flagged for a future PHASE_7 §5.2 wording fix ("`envelope.*` = `attack`/`decay`/`sustain`/`release`/`attackCurve`").
- **Status:** open

### C-14: the sound stage's `SoundDesign.TrackSound` carries a `midi` field beyond §7's printed shape (the drum trigger-pitch delivery channel §7 left unspecified)
- **Date / session:** 2026-07-18, session 14 (Phase 7, chunk 2), T2 the atomic flip
- **What deviated:** PHASE_7 §7 enumerates the sound stage's output as `SoundDesign = {trackSounds: {trackId: {instrument, effects, channel, sends}}, buses, master}` — the per-track `TrackSound` shape is printed as exactly `{instrument, effects, channel, sends}`, with no `midi`. But drum `PhraseNote`s are generated with `midi=None` (`parts/generators.py`: "Phase-7 timbres supply the pitch"), and the Serializer must inject each drum track's trigger pitch from the timbre (PHASE_5 §8.2 reserved the trigger `midi` to this phase; PHASE_7 §4.3/D13 pin the kit as its owner). The provisional stub delivered it via `pipeline/stubs.py::TrackSound.midi`; §7's real `TrackSound` shape has no such channel. Resolution: `sound/stage.py::TrackSound` carries `midi: int | None = None`, populated per drum voice from `KitVoice.midi` (pitched roles + NoiseSynth voices keep `None`); the Serializer reads `design.track_sounds[id].midi` exactly as it read the stub's.
- **Why:** the trigger pitch has to reach the Serializer through the sound stage's output (the stage is the only thing that resolves the flavor → kit → per-voice midi), and §7's printed `TrackSound` enumeration simply omits it — an unspecified integration gap, not a contradicted pinned value. D13/§4.3 already pin the kit as the midi owner, so carrying it on the stage's per-track output is the faithful mechanism (the C-11 / C-01 pattern of filling an unspecified integration point).
- **Impact:** `src/trackgen/sound/stage.py` (`TrackSound.midi` + its population), consumed by `src/trackgen/pipeline/serialize.py`. **Internal IR only** — `SoundDesign`/`TrackSound` is the stage→Serializer handoff type, not the client-facing `TrackDocument`; the trigger midi flows into the pinned `NoteEvent.midi` exactly as before (the whole-document goldens' drum trigger midis are unchanged: kick 24, hats 80, ride 82, crash 84, toms 43/47/50, snare/perc NoiseSynth → none). The client contract is untouched. A later phase reading `SoundDesign.TrackSound` must know it carries `midi` (drum voices only).
- **Doc amendment:** none applied — §7 governs the mix/routing surface and did not enumerate trigger-midi delivery, so this fills an unspecified gap rather than changing a pinned value. Flagged for a future PHASE_7 §7/§4.3 clarification to state the `TrackSound.midi` field explicitly.
- **Status:** open

### C-13: PHASE_7 §9.2's printed jazz `upright` `envelope.attack` sample was computed with the wrong directive scalar (corrected to the faithful engine value)
- **Date / session:** 2026-07-18, session 14 (Phase 7, chunk 2), T1 §9 stage goldens
- **What deviated:** PHASE_7 §9.2's worked-example table printed the jazz `upright` bass `envelope.attack` as `0.05 × 0.04^0.333 ≈ 0.0171 s`. That param is mapped by **attackHardness** (§8.2 `upright.mod.attackHardness: {param: envelope.attack, min: 0.05, max: 0.002, curve: exp}`), and jazz/melancholic's attackHardness is **0.32**, not brightness 0.333 — the printed formula used the brightness scalar as the exp exponent. The faithful engine value is `round3(0.05 × (0.002/0.05)^0.32) = round3(0.01785) = **0.018 s**`. (§9.2's `modulationIndex = 1.5×4^0.333 ≈ 2.380` is unaffected — modulationIndex genuinely maps under brightness=0.333, so that sample is correct.)
- **Why:** golden-value arbitration (ROADMAP §3 rule 2) — the §9 worked-example numbers are derived samples; on divergence the pinned algorithm/data text (here §8.2's unambiguous attackHardness mapping) wins and the wrong sample is amended. The engine (`sound/stage.py` via `evaluate.apply_directives`) faithfully applies attackHardness=0.32; only the printed digit was wrong. This is the C-09 pattern (a wrong derived sample, no engine bug). **User signed off 2026-07-18** on amending §9.2, engine unchanged.
- **Impact:** `plans/PHASE_7.md` §9.2 table amended (`0.05×0.04^0.333 ≈ 0.0171` → `0.05×0.04^0.32 ≈ 0.018`); the T1 stage golden `tests/test_sound_stage_goldens.py::test_jazz_9_2_field_for_field` asserts the faithful **0.018** as the committed regression surface (arbitration rule 3). One **downstream chained sample** carries this value: the jazz whole-document milestone fixture (`fixtures/jazz.milestone.trackdoc.json`), re-blessed in session-14 T3 — its `bass` instrument `envelope.attack` will read 0.018. No other §9 value chains from this one (each mapped param evaluates independently). No engine source changed.
- **Doc amendment:** applied in the T1 commit alongside the recomputed §9.2 stage golden (arbitration rule 2 — doc amended in the same commit as the fixture asserting the corrected value); the initially-`xfail`ed "matches printed sample" test was deleted once the doc was corrected (there is no longer a mismatch to document).
- **Status:** resolved

### C-12: the §3.7 entry-crash velocity has no floor, so `crash.velocity` `lo == 0` entering an `energy == 0` section would emit a velocity-0 `PhraseNote` (latent `PhraseNote.velocity gt=0` crash)
- **Date / session:** 2026-07-17, session 12 (Phase 6, chunk 3), whole-phase correctness review
- **What deviated:** PHASE_6 §3.7 pins the entry-crash velocity as `round3(lo + energy × (hi − lo))` with no lower floor, and §4.1 **TR1** explicitly permits `crash.velocity` `lo == 0` (`0 ≤ lo ≤ hi ≤ 1`); `FormSection.energy` permits `0.0`. But `PhraseNote.velocity` is `Field(gt=0, le=1)` (`schema/ir.py:232`). So a pack authoring `crash.velocity: [0.0, hi]` whose fill/stop boundary *enters* a section of energy exactly `0.0` yields crash velocity `0.0` → `add_crash_and_kick` (`transitions/_common.py`) builds `PhraseNote(velocity=0.0)` → pydantic `ValidationError` (uncaught pipeline crash), rather than a clean error. The **HOLD** crash escapes this because §3.6 adds `+0.05` before building the note; only the §3.7 **entry** crash has no floor.
- **Why:** logged, not fixed, because closing it means changing a pinned value — either flooring the §3.7 formula (e.g. `max(0.05, …)`, a formula change) or tightening **TR1** to require `lo > 0` (TR1 currently pins `lo ≥ 0`). Both are design decisions needing sign-off, and the edge is unreachable in v1: both reference packs use `lo` 0.55 (pop) / 0.40 (jazz), so `crash_velocity` is always ≥ 0.40. This is the **C-10 pattern** — a latent gated behind future pack authoring.
- **Impact:** `src/trackgen/transitions/_common.py::crash_velocity` (+ `add_crash_and_kick`). **Latent only** — no v1 pack can reach it (verified: the DoD-9 property matrix over both reference packs × all moods × lengths × 25 seeds produces zero validation failures). A Phase-8 pack authoring `crash.velocity` `lo == 0` (plausible for an ambient/lo-fi "no crash at zero energy" intent) would crash the pipeline on a zero-energy fill/stop entry. Closing it: floor the entry-crash velocity at `PhraseNote`'s minimum, or add a loader guard requiring `crash.velocity` `lo > 0` (with a §3.7/TR1 amendment + sign-off). Adjacent to C-10 (both are latent validation edges unreachable in v1).
- **Doc amendment:** none applied — §3.7/TR1 as pinned permit the combination; resolving it changes a pinned value, deferred to a future §3.7/§4.1 pass (or Phase-8 pack authoring that would first reach it). Flagged for sign-off then.
- **Status:** open

### C-11: drum notes carry internal voice/`ornament` provenance tags beyond the §3.9 four (the mutation pass needs provenance the `PhraseNote` IR lacks)
- **Date / session:** 2026-07-17, session 10 (Phase 6, chunk 1), T3 mutation + whole-chunk review
- **What deviated:** PHASE_6 §3.9 lists the tags stage 6 *contributes to output* as exactly `"fill"`, `"crash"`, `"var"`, `"hold"`. But two §3.7 mutation operators need drum-voice/ornament provenance that the pinned `PhraseNote` IR (PHASE_1 §4.5: `ticks`/`durationTicks`/`midi`/`velocity`/`tags`) does not carry: **`hat_lift`** must find `hat_closed` specifically (both `hat_closed` and `hat_open` collapse to the single `"hats"` voice-track via `generators._VOICE_TRACK`), and **`drop_ornament`** must find `minDensity` events (consumed at instantiation, §3.5). Resolution: `generators._generate_drums` tags each instantiated drum note with its source **voice** (e.g. `"hat_closed"`/`"hat_open"`/`"kick"`…) and marks `minDensity`-carrying events `"ornament"`; the mutation reads these, and `hat_lift` rewrites the voice tag when it lifts. These are **internal engine provenance**, not contributed output tags.
- **Why:** the pinned `transitions(phrases, form, chords, arr, plan, pack)` signature (§3) cannot take `selection`, so the stage cannot re-derive which pattern/events a section used; the note set is the only channel. Carrying provenance as tags at generation time is the minimal faithful mechanism and matches the design's "pre-declared droppable" framing of the ornament class (§3.7).
- **Impact:** `src/trackgen/parts/generators.py` (`_generate_drums`), consumed by `src/trackgen/transitions/mutation.py`. **Serialize-invisible:** `NoteEvent` (PHASE_1 schema) has no `tags` field and `pipeline/serialize.py::_to_event` copies only ticks/dur/midi/velocity, so these tags never reach `TrackDocument` — the client contract is untouched and every whole-document golden is unchanged (verified). No Phase-5 phrase golden broke (they use tag-membership checks, not `tags == []`). A later phase reading a drum `PhraseNote.tags` must know drum notes now carry a voice tag (+ maybe `"ornament"`) as their first entries. A future PHASE_6 §3.9 / §4.5 pass could state the internal-provenance-tag convention explicitly (or the IR could gain a `voice` field).
- **Doc amendment:** none applied — §3.9 governs *contributed output* tags and these are internal/serialize-dropped, so this fills an unspecified integration gap rather than changing a pinned value. Flagged for a future §3.9/§4.5 clarification.
- **Status:** open

### C-10: the thin Serializer does not de-duplicate coincident same-voice drum triggers (latent V3 edge)
- **Date / session:** 2026-07-17, session 09 (Phase 5, chunk 4), whole-phase correctness review
- **What deviated:** PHASE_5 §8.3 pins the Serializer as *thin* — concatenate a track's Phrases, sort `(ticks, midi)`, clamp/truncate — and is silent on de-duplication. Neither the Serializer (`pipeline/serialize.py`) nor the drum generator (`parts/generators.py::_generate_drums`) nor a loader PT-rule forbids two drum events at the **same `pos`** mapping to one track. For a **NoiseSynth**-backed drum track (the snare in the reference kit) that produces two `NoteEvent`s at one tick, which trips validator **V3** ("duplicate ticks (double-hit)", `schema/validate.py`). Resolved reading: the thin Serializer trusts non-colliding drum authoring; coincident same-voice de-dup is **not** performed in v1.
- **Why:** §8.3's "thin" intent + the reference banks avoiding the collision purely by **authoring convention** (e.g. `pr_dr_3` deliberately omits a 4& `hat_closed` that would coincide with its `hat_open`). A 720-document fuzz (both packs × 6 moods × 5 lengths × 12 seeds) produced **zero** V-rule violations, so the edge is unreachable through `generate_track` in v1 — adding a de-dup or a loader PT-check at the milestone close-out would be unpinned surface for no reachable benefit.
- **Impact:** `pipeline/serialize.py` / `parts/generators.py`. **Latent only.** A future/degenerate drum pattern authoring two same-voice hits at one `pos`, or a pack backing the shared `hats` track (closed+open at one `pos`) with a NoiseSynth patch instead of MetalSynth, would serialize a V3-invalid document. Closing it is a one-line coincident-tick de-dup in the drum-track builder, or a loader PT-rule forbidding coincident same-voice drum events — a natural Phase-6 item (Phase 6 authors fills/variation in `parts/` and can add the guard where it edits). Adjacent to but not covered by C-07 (§3.3 sub-60 drop) or C-08 (ride band prose).
- **Doc amendment:** none applied — §8.3 does not contemplate de-dup, so this resolves an unspecified point rather than changing a pinned value. A future PHASE_5 §8.3 / §5.5 pass could state the no-coincident-same-voice-drum-event convention (or add the PT-check) explicitly.
- **Status:** open

### C-09: PHASE_5 §9/§13.5 worked-example samples corrected to the faithful engine (7 wrong derived samples)
- **Date / session:** 2026-07-16, session 08 (Phase 5, chunk 3), T4 goldens + golden-value arbitration
- **What deviated:** seven printed worked-example values in PHASE_5 §9.2/§9.3/§9.4 (and the §13.5 DoD note-count list) diverged from the faithful, frozen engine when the T4 goldens drove the real chained pipeline (master 3735928559, seed `1ps9wxb`). A deep investigation (trace scripts, DP-cost enumeration) confirmed **all seven are wrong derived doc samples — NO engine bug**; the committed walker/voicing/generator code faithfully implements the pinned §6.3/§6.4/§6.5/§3.6/§8.6 text. Corrections: **(RC1)** §9.2 solo note counts `50/53/53` → `51/54/54` (also §13.5) — §6.3 rule-6 embellishment fires on every `barInSection ≡ N−1`, the section's **final** bar included (the sample dropped the final-bar ghost); draw-free, so the 128 draw total is unchanged. **(RC2)** §9.2 solo-1 bar-13/bar-15 beat-4 approach pitches `C2`/`G♭1` → `D♭2`/`F1` — the approach-type draw materializes candidates in **ascending pitch order** (§3.6 line 293, unqualified), which the engine applies uniformly; the old samples matched an `approachWeights` map-order the reference script apparently used (both orderings give identical type probabilities, so it is a determinism convention only). **(RC3)** §9.3 jazz solo rootless + pop comping voicings settle an octave higher than printed (e.g. Dm9 `C3 E3 F3 A3` → `C4 E4 F4 A4`; B♭13 `D3 F3 A♭3 G4` → `D4 F4 A♭4`, a 3-voice rootless since B♭13 has no 9th; pop verse/chorus E `E3 G♯3 B3` → `G♯3 B3 E4`) — these are the **verified global minimum** of the frozen Phase-4 Viterbi DP (the printed lower octaves are drift-tied but cost +371/+1526 more on global voice-leading); the "3 of 4 voices static" prose corrected to "2 of 4"; register-determinate voicings (jazz head shells, pop pads `fifths`) reproduce unchanged. **(cascade)** §9.4 pop verse-1 bar-4 comping `E3+G♯3+B3` → `G♯3+B3+E4` (inherits the §9.3 verse-1 E voicing; durations/timing/velocities unchanged).
- **Why:** golden-value arbitration (ROADMAP §3): the §9 worked-example numbers are derived samples from a reference script not in the repo; on divergence the pinned algorithm text wins and the wrong sample is amended. The user signed off on all seven amendments and ruled RC2 explicitly (ascending-pitch ordering is authoritative → engine unchanged, samples amended).
- **Impact:** `plans/PHASE_5.md` §9.2/§9.3/§9.4/§13.5 amended to match the engine; the T4 goldens (`tests/test_walker_goldens.py`, `test_voicing_goldens.py`, `test_generator_goldens.py`) assert the corrected values as the committed regression surface (arbitration rule 3). **No engine source changed** — walker/voicing/generators are exactly as landed in `a93c1a6`/`b9eb7aa`/`fa00f51`. Any later phase re-deriving these examples must use the corrected values.
- **Doc amendment:** applied in this commit (PHASE_5.md amended alongside the recomputed T4 fixtures, per arbitration rule 2 — same commit).
- **Status:** resolved

### C-08: jazz ride "skip" notes are authored below the §5.2 ride velocity band (0.65–0.75)
- **Date / session:** 2026-07-16, session 06 (Phase 5, chunk 1), T2 reference banks
- **What deviated:** PHASE_5 §5.2 states the jazz ride band as "0.65–0.75". The §7.4 golden anchor `jz_dr_2` (fully stated, reproduced verbatim) authors the ride line `1, 2, 2&, 3, 4, 4&` as `0.70, 0.72, 0.55, 0.70, 0.72, 0.55` — the two "skip" notes (the `&` of 2 and 4) sit at **0.55**, below the stated band; the completed `jz_dr_4` similarly uses 0.60 for its skip notes. The "above the snare/kick comping hits (0.3–0.5)" relationship §5.2 also states still holds.
- **Why:** the band imprecision is in the pinned doc itself, not an implementer choice — 0.55 comes straight from the §7.4 golden anchor, which the reference bank reproduces verbatim (per golden-value arbitration, the normative fixture wins over the prose band). The band was written for the accented (downbeat) ride notes; the swung skip note is idiomatically lighter.
- **Impact:** `styles/jazz/patterns/drums.yaml` (`jz_dr_2` skip notes 0.55, `jz_dr_4` 0.60; session 19 adds `jz_dr_4b` 0.62 under the same idiom). A future §5.2 pass should clarify the band applies to accented ride notes and that skip notes go lower, so a validator or reviewer does not false-alarm on the 0.55/0.60/0.62 values. No behavior change; the values are the golden.
- **Doc amendment:** none applied — the §7.4 anchor is the binding data; §5.2's band is descriptive prose. Flagged for a future §5.2 clarification.
- **Status:** open

### C-07: §3.3 retargeting — three under-specified points resolved (guide3-over-sus, guide7 fallback scope, <60-tick drop on all retrigger segments)
- **Date / session:** 2026-07-16, session 06 (Phase 5, chunk 1), T3 foundations
- **What deviated:** PHASE_5 §3.3's degree table + `onChordChange` prose left three points under-specified; `parts/retarget.py` resolves them as: **(a)** `guide3` over a **suspended** chord (which has no guide third) → the `third` slot (the suspension tone); §3.3's `guide3` fallback cell is "—", implicitly assuming a third always exists. **(b)** `guide7` fallback broadened from the literal "triads → the fifth" to **any seventh-less quality** (including maj6/min6) → the fifth; note the intended asymmetry that `seventh` on maj6/min6 falls back to the 6th while `guide7` on maj6/min6 falls back to the fifth (guide tones are a distinct concept). **(c)** the `retrigger` "remainders < 60 ticks are dropped" rule is applied to **every** split segment, not only the trailing remainder — so a note < 120 ticks straddling a chord boundary can split into two sub-60 pieces and emit **zero** notes (the whole note vanishes).
- **Why:** (a)/(b) are dressing-safe gap-fills — dressing (PHASE_4 §6) changes qualities per mood, so a degree must resolve for every quality a pattern might meet, including sus and 6-chords the fallback column doesn't enumerate. (c) treats any sub-60-tick fragment as inaudible regardless of position, consistent with the rule's intent (drop inaudible fragments).
- **Impact:** `src/trackgen/parts/retarget.py`. (c) is a **latent** edge only: the reference banks' shortest retargeted pitched note is the 120-tick 16th pickup (`pr_bs_4`), which can lose ≤ 59 leading ticks at a boundary but never fully vanishes; a future pack authoring a < 120-tick pitched note on a chord boundary would see it disappear. Pinned by `tests/test_foundations.py::test_on_chord_change_retrigger_drops_all_short_segments`. Chunk 3 generators consume these resolutions as-is; a future PHASE_5 §3.3 pass could enumerate the sus/6-chord guide-tone fallbacks and clarify the drop scope explicitly. The `push` tag is emitted even when no boundary falls in the note's span — this is NOT a deviation (§3.3 states flatly "Pushed notes are emitted with tag `push`").
- **Doc amendment:** none applied — these fill unspecified cells / clarify intent rather than change a pinned value. Flagged for a future §3.3 pass.
- **Status:** open

### C-06: Phase-5 pattern-bank rules PT5/PT6/PT7/PT10 are enforced only for packs declaring a Phase-5 marker
- **Date / session:** 2026-07-16, session 06 (Phase 5, chunk 1), T1 loader
- **What deviated:** PHASE_5 §5.5 reads PT10 ("`layeringOrder` present once per pack") and PT6/PT7 ("`mode`/`voicing` required") as **unconditional**, and PT5 (completeness) as applying to any "role with a pattern bank." The loader (`packs/loader.py::_check_pattern_banks`) instead fires PT5/PT6/PT7/PT10 **only when the pack declares at least one Phase-5 marker** (`layeringOrder`, bass `mode`/`walking`, or comping/pads `voicing`). Packs with none — `styles/_stub` (a Phase-1 envelope-violation test fixture) and the reference packs *before T2 authors them* (empty `patterns: []` banks) — load on the legacy Phase-1 flat-envelope path and skip those four whole-bank rules (they still get per-entry PT1/PT2/PT3/PT4/PT8/PT9/PT11).
- **Why:** ~dozens of Phase 1–4 suite tests call `resolve_pack("pop_rock"/"jazz")`, whose pattern banks are empty until T2 (a later task, out of T1's file scope); and the SESSION_06 T1 contract requires keeping `_stub` + all 644 prior tests green. Enforcing the four rules unconditionally would reject those packs at load. `_stub` legitimately has no generation intent, so requiring `layeringOrder`/`voicing` on it is meaningless.
- **Impact:** `src/trackgen/packs/loader.py`. Once T2 authors `pop_rock`/`jazz` with `layeringOrder` + bass `mode` + `voicing` (all present in §7), `phase5=True` and **all four rules fire** on the real reference packs (verified premise: T2's clean-load exercises the enforced path). A pack that authors real patterns but declares **no** marker would escape PT5/PT10 — but it cannot generate (`arrange()` needs `layering_order`, bass gen needs `mode`, comping/pads need `voicing`), so it fails loudly downstream, not silently. A future session (or Phase 8 pack authoring) that wants unconditional enforcement should special-case `_stub` instead of gating on markers. Also latent: PT1's "whole number of bars" is checked against a fixed `_TICKS_PER_BAR = 1920` constant (v1 is 4/4-only per PHASE_1; the envelope model carries no manifest handle).
- **Doc amendment:** none applied — §5.5 does not contemplate the transitional empty-bank state, so this fills an unspecified gap rather than changing a pinned condition. A future PHASE_5 §5.5 pass could state the marker-gating (or exempt `_stub`) explicitly.
- **Status:** open

### C-05: pattern-event authored order need not be non-decreasing in `pos` (PT2)
- **Date / session:** 2026-07-16, session 06 (Phase 5, chunk 1), T1 pattern schema
- **What deviated:** PHASE_5 §5.5 **PT2** reads "events ordered by `pos` **as authored**". T1 first implemented this as a hard *non-decreasing-`pos`* authoring constraint, which the T1 review showed **rejects the normative §7 reference patterns** — they are authored **voice-grouped** (e.g. §7.1 `pr_dr_2a` has pos `0, 960, 480, 1440, 0, 240, …`, decreasing across voices). §7 declares those fixtures "normative golden-test data," and §14/DoD-1 requires the reference packs to load clean, so the strict reading is refuted by the pinned data. PT2 was relaxed to validate only `pos ∈ [0, lengthTicks)`; the authored order is treated as authoritative (generators sort notes `(ticks, midi)` at emit time, §6), not required to be pre-sorted.
- **Why:** golden-value arbitration (ROADMAP §3) — where algorithm/rule text conflicts with the normative worked data, the data wins. "ordered by `pos` as authored" is compatible with (indeed better read as) "the authored `pos` values are authoritative," not "authored events must already be sorted." A non-decreasing constraint would make the normative §7 banks unloadable.
- **Impact:** `src/trackgen/packs/models.py::PatternEnvelope._check_envelope`. Downstream part generators (chunk 3) must not assume authored events are pos-sorted; they sort at emit time (already the pinned §6 behavior). A regression test (`tests/test_patterns_pack.py::test_pt2_voice_grouped_events_load`) loads the literal §7.1 `pr_dr_2a` pos sequence to guard this.
- **Doc amendment:** none needed — the relaxed reading is consistent with §5.5's "as authored" text and required by the normative §7 fixtures; no pinned value changes.
- **Status:** resolved

### C-04: theory voicing API — `voicing_candidates` has no key (quartal reading) and `optimal_voicing_path` gains an additive `anchor` param
- **Date / session:** 2026-07-16, session 04 (Phase 4, chunk 1), T2 voicing
- **What deviated:** The §8.3 pinned signature `voicing_candidates(spec, cls, lane)` carries no key, but §8.4's `quartal` class is defined as "three stacked 4ths **from the scale (§7.4)** starting on a chord tone" — the scale is unresolvable without a key. Implemented as **perfect** fourths from the root (`[0, 5, 10, 15]`, four voices), the only key-free reading. Separately, §8.6's `optimal_voicing_path` drift term needs the lane anchor, absent from the pinned signature; implemented as an **additive keyword-only** `anchor: int | None = None` (default derives the candidate-range midpoint, recovering the lane midpoint when candidates fill the lane), so the pinned positional signature is preserved and Phase 5 injects the real anchor.
- **Why:** the §8.3 signatures and the §8.4/§8.6 prose are mutually under-specified on the key/anchor; both readings are the minimal key-free / additive resolutions. No golden in this chunk exercises quartal (its voicing goldens land in PHASE_5 §13.6), so nothing is pinned against these choices yet.
- **Impact:** `src/trackgen/theory/voicing.py`. Phase 5 owns candidate-class-per-role policy and the quartal/fifths voicing goldens (PHASE_5 §13.6) — that session should confirm the perfect-4th quartal reading (or widen the signature to pass a key/scale) and pass the real lane anchor. Also latent: `rootless_a`/`rootless_b`/`drop2` on **triads** degrade to fewer voices than their 4-note formulas imply (deterministic; the DP pads mixed cardinality) — not pinned; Phase 5 policy decides class-per-role so triads never hit seventh-chord classes.
- **Doc amendment:** none applied (deferred to the PHASE_5 §13.6 decision; both readings documented in the `voicing.py` module docstring). Additive/under-specification resolutions, not a change to any pinned value.
- **Resolution (2026-07-16, session 08, Phase 5 chunk 3, T1 voicing pass):** the three under-specified points are confirmed and now exercised by the §13.6 goldens — **RESOLVED**. (1) **Keyless `quartal` = perfect fourths** `[0,5,10,15]` kept; jazz pads are the only quartal user and are dormant in v1 (`layersMax` 3), so no §9.3 golden pins it — a diatonic-quartal widening is deferred to Phase 8. (2) **The real lane anchor `lane.high − 6`** is passed explicitly by the Phase-5 voicing pass (`parts/voicing.py`), verified against §9.3's stated anchors (jazz comping lane 46–69 → 63, pop comping 50–71 → 65). (3) **Candidate class per role** comes from pack data (`voicing.classes[rung]`): pop comping uses `triad_close/triad_open/shell3`, jazz uses `shell2/shell3/rootless_*`, so a triad never routes into a 4-note seventh-chord class. The mixed-cardinality padding (`_pad_to_equal` pads the shorter voicing with its own **top** pitch, §6.4 amendment 10) is now directly asserted (`tests/test_voicing_pass.py::test_pad_to_equal_pads_with_own_top_pitch`). The frozen `theory/voicing.py` was **not** changed — all three resolutions are Phase-5 *caller policy* over the pinned signatures, exactly as C-04 anticipated.
- **Status:** resolved

### C-03: P8 admits the tritone-substitute dominant (bII7), beyond the literal §3.2 "D-function"
- **Date / session:** 2026-07-16, session 04 (Phase 4), T4 progressions loader
- **What deviated:** PHASE_4 §4.3 **P8** pins "turnarounds … final chord D-function" and §14 DoD-1 requires the §9.2 reference pack to **load clean**. These two pins conflict: §9.2's `tritone_turn` ends on `bII7`, but the §3.2 function table maps degree `b2 → S`, so a literal `chord_function(last) == "D"` check rejects the reference pack. The loader implements P8 as "final chord is dominant-functioning" = `chord_function == "D"` **OR** a dominant-seventh rooted on `b2` (the SubV / tritone substitution, which resolves to I exactly as V does).
- **Why:** the verbatim-reference-loads-clean requirement (D-S4 discipline + DoD-1) is the binding tie-breaker; `bII7` is functionally the dominant in a turnaround relaunch (it is the corpus-standard tritone sub), and the §3.2 table's generic `b2 → S` does not model the substitution context. The narrow admission (dom7-family rooted on b2 only) keeps P8 otherwise strict — it still rejects a turnaround ending on a tonic or plain subdominant (tested).
- **Impact:** `src/trackgen/packs/models.py::_relaunches_as_dominant` carries the reconciliation and a pointer to this caveat. It admits any dom7-family chord **rooted on pitch-class 1** (i.e. `bII7` or its enharmonic `#I7`) — the SubV is a pitch-defined function, so the pc test is intentionally slightly broader than the "b2 spelling" prose. Any Phase 8 pack authoring a turnaround that ends on a non-dominant, non-SubV chord will still be rejected. If a future function-table revision assigns `b2 → D` for tritone subs, this special case becomes redundant and can fold back into the plain `chord_function == "D"` check. The runtime turnaround transform (§5.4, Phase 4 harmony stage — not built in T4) should use the same "dominant-functioning relaunch" notion when it validates/consumes turnarounds, not the raw §3.2 label.
- **Doc amendment:** none applied (design text left as pinned; flagged for reviewer). A future PHASE_4 §4.3 pass should either (a) reword P8 to "dominant-functioning (D-function or SubV)", or (b) extend the §3.2 table's b2 row — either makes the doc self-consistent.
- **Status:** open

### C-02: the degradation ladder (§7.3) is unreachable under the pinned §5.2 + §7.1 rules
- **Date / session:** 2026-07-15, session 03 (Phase 3)
- **What deviated:** PHASE_3 §7.1 step 5 / §7.3 present the `degrade` ladder as reachable ("exists for budgets too small for the template's drawn/minimal configuration"), and DoD §11.7 asks for "a fixture exercising each degrade op class." In the faithful implementation the ladder is **provably unreachable through `form()`**: §5.2 gates a template *in* only if `minBars ≤ barBudget` (its all-smallest config fits); §7.1 step-3 resolves each bar count only to options keeping `minimalTotal ≤ barBudget`, and the smallest option never raises `minimalTotal`, so the "0 feasible → take smallest" branch never fires and `minimalTotal ≤ barBudget` holds throughout; the arithmetic repeat count then yields `total ≤ barBudget` even when clamped up to `count.min` (that case gives `total == minimalTotal`). So the step-5 loop guard `total > barBudget` is never true for a *selected* template; the only over-budget path (no template eligible) routes to the step-6 **fallback**, not the ladder. Confirmed empirically: 0 ladder ops across ~40k property-matrix runs, and two independent review agents reconstructed the proof.
- **Why:** the pinned eligibility filter (§5.2, D4) and the pinned feasibility filter (§7.1 step 3b, D13) together guarantee fit before the ladder can ever run — the ladder and the eligibility/feasibility gates are two mechanisms solving the same "make it fit" problem, and the gates always win first. The step-3b "0 feasible" case the ladder was meant to repair is itself unreachable because the smallest authored option is always feasible.
- **Impact:** the ladder code (`src/trackgen/form/stage.py`, the step-5 branch) is retained as **defensive/unreachable** and is correct if ever reached (drop/shrink/dropFromRepeat verified by inspection). DoD §11.7's "exercise each degrade op class" was satisfied via a **substitute**: `test_degrade_ladder_authored_order` asserts each reference pack's authored ladder covers all three op classes in D11 order (data/config level); `test_30s_at_slow_tempo_valid_form` + tiny-budget fallback tests cover the fallback; a full-grid regression guard asserts the ladder never fires and the hard ceiling always holds. A future change that could make the ladder live — e.g. loosening §5.2 eligibility, or a pack/rule that lets an over-budget template be *selected* — must add real ladder-execution coverage. Two latent, currently-moot validation gaps in the ladder's own input rules were left unfixed for the same reason (they guard the dead path): F8's ending-candidate set omits the section a `drop` op newly exposes as final, and F9 accepts a top-level `drop:` targeting a repeat-only type (a silent no-op at runtime). Revisit both if Phase 8 authoring ever makes the ladder reachable.
- **Doc amendment:** PHASE_3 §7.3 amended (post-review, 2026-07-15) with an "Implementation note (v1, defensive/unreachable)" paragraph documenting the eligibility+feasibility proof and the white-box substitute for §11.7. Not an algorithm change — the fitter's behavior is unaffected.
- **Resolution (post-review, 2026-07-15):** user signed off on option A — keep the ladder as defensive code. The step-4/5 logic was extracted to a module-level `_fit_and_degrade` (`src/trackgen/form/stage.py`) and its drop/shrink/dropFromRepeat op semantics + authored-order/exhaustion control flow are now locked by direct white-box unit tests (`tests/test_form.py::test_ladder_*`), converting §11.7's "verified by inspection" into executable coverage. The two latent F8/F9 input-validation gaps remain intentionally unfixed (they guard the dead path); revisit only if Phase 8 makes the ladder reachable.
- **Status:** resolved

### C-01: `PARAM_MALFORMED` structural error code added beyond the §3.1 catalog
- **Date / session:** 2026-07-15, session 02 (Phase 2)
- **What deviated:** PHASE_2 §3.1 pins a 14-code validation catalog (all semantic conditions, assuming well-typed JSON input). The implementation adds a 15th code, `PARAM_MALFORMED`, emitted by `generate_plan` when `Params.model_validate` rejects a malformed field *type* (e.g. a fractional `tempoBpm`, a non-mapping `key`/`roleFlavors`). `validate_params` (the §3.1 catalog function) is unchanged and still returns only the 14 documented codes.
- **Why:** `validate_params` operates on the raw client dict and deliberately defers type/structural checks to the pydantic `Params` model (D-S6 two-layer design). Without wrapping, a malformed-type request escaped the public `generate_plan` boundary as a raw `pydantic.ValidationError` — contradicting §3.1's "failures return the full list of errors." Wrapping into `ParamsInvalid` with a structural code keeps a single structured error type at the boundary without fabricating a *semantic* §3.1 code.
- **Impact:** A later client-contract/HTTP layer enumerating error codes must include `PARAM_MALFORMED` (structural) alongside the 14 semantic §3.1 codes. Its `field` is a pydantic dotted location (e.g. `tempoBpm`), and its `message` is the pydantic message (not a hand-authored catalog message). Purely additive — no existing code changes meaning.
- **Doc amendment:** none needed — §3.1 does not specify behavior for malformed input types, so this fills an unspecified gap rather than changing a pinned condition. A future PHASE_2 §3.1 pass could document it explicitly.
- **Status:** resolved
