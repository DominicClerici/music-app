# SESSION_12 — Phase 6, Chunk 3 (wiring + milestone + whole-phase)

**Phase 6 = Transitions, Variation & Humanization. This is the FINAL chunk.** The stage-6
Transition engine (`src/trackgen/transitions/`, Chunk 1) and the stage-7 Humanizer
(`src/trackgen/humanize/`, Chunk 2) both exist as real, tested functions but are **NOT yet wired**
into the pipeline. Chunk 3 wires them, re-blesses the two whole-document goldens (the output
legitimately changes), proves the whole-phase property matrix (DoD 9), runs the milestone audition
(DoD 10), and closes Phase 6 with a whole-phase 4-lens review + full §11 DoD 1–11 + the §10 amendment
audit (DoD 11).

Read before implementing: **PHASE_6 §3 / §5 / §6 / §7.3 / §8.3 (D6/D17) / §10 / §11** (binding);
PROGRESS.md handoff block (the 7 C3 build items + escalation watch); CAVEATS C-10, C-11.

**Approved 3-chunk split is fixed — do NOT re-plan it.** This session plans only C3's tasks.

---

## Session scope

**In scope (delivers DoD 9, 10, 11):**

1. **Wire the two real stages** into `pipeline/orchestrator.py`, deleting the `transitions`/`humanize`
   stubs (keeping `sound_design` — Phase 7).
2. **Thread `tempoEvents`** from `humanize`'s 2nd return through the orchestrator into the Serializer
   (`header.tempos = [Tempo(ticks=0, bpm=plan.tempo_bpm)] + tempoEvents`).
3. **Crash Serializer + stub timbre**: `_STUB_MIX["crash"]` entry; a `crash` entry in both packs'
   `timbres.yaml` drum kits (trigger midi **84**); remove the `crash`-skip guard in `stubs.sound_design`.
4. **Re-bless** both `fixtures/{pop_rock,jazz}.milestone.trackdoc.json` (+ `test_whole_document_goldens.py`)
   in a **dedicated commit** (T2), re-verifying V1–V8, the §9.4 anchors, the §7.3 milestone facts, and
   the jazz **40-entry** `header.tempos` (base + 39 ritard).
5. **Whole-phase property matrix (DoD 9)** — both packs × supported moods × lengths × 25 seeds through
   the fully-wired pipeline: §11.9 checks + confirm the **P1 latent** and **C-10** stay unreachable.
6. **Milestone (DoD 10)** — both examples regenerate through the real stages and play in the Phase-1
   playground; the §11.10 listening checklist. **Needs a user audition** (like PHASE_5 §9.5).
7. **Whole-PHASE 4-lens review** (across all 3 chunks) + **full §11 DoD 1–11** + **DoD 11 / §10
   amendment audit**. CAVEATS for any deviation.

**Out of scope:** any change to stage-6 or stage-7 internals (frozen this chunk — Chunk 1/2 are DONE and
their goldens must stay green); `sound_design` mix/bus logic (Phase 7); new pack content beyond the one
`crash` stub-timbre row per pack; any re-derivation of already-proven §7.1/§7.2 samples.

## Contracts consumed / produced

- **Consumes** the real stage signatures (both already committed, tested):
  - `transitions(phrases, form, chords, arr, plan, pack) -> list[Phrase]` (`trackgen.transitions`).
  - `humanize(phrases, form, plan) -> tuple[list[Phrase], list[Tempo]]` (`trackgen.humanize.stage`).
  - Orchestrator already holds every argument: `sf`=form, `hp`=chords, `ap`=arr, `plan`, `pack`.
- **Produces** the wired pipeline: `generate_track(raw_params) -> TrackDocument` now emits fills, entry
  crashes, mutation, HOLD endings, humanized feel, and (jazz) a multi-event tempo map. The
  `TrackDocument` schema is **untouched** (`header.tempos` was multi-event from PHASE_1).

## Authoritative wiring facts (verified this session against the code)

- `pipeline/orchestrator.py:75-79` currently calls the stubs and **discards** `humanize`'s 2nd return
  (`phrases, _tempo_events = humanize(phrases)`); `serialize(...)` takes no tempo events.
- `pipeline/serialize.py`: `serialize(plan, form, phrases, patches, *, params=None)`; builds
  `tempos=[Tempo(ticks=0, bpm=plan.tempo_bpm)]` (line 94). `_STUB_MIX` (lines 38-50) has **no `crash`
  row**; `_build_track` does `volume_db, pan = _STUB_MIX[track_id]` (KeyError on crash without it).
  `_EMIT_ORDER = (*_TRACK_ORDER, "bass", "comping", "pads")` — **`crash` is already in `_TRACK_ORDER`**
  (`generators.py:103`), so `_EMIT_ORDER` already emits crash; the *only* Serializer gaps are the
  `_STUB_MIX` row and the tempo-events input.
- `parts/generators.py`: `crash` is already in `_VOICE_TRACK` (→ `"crash"`) and `_TRACK_ORDER`; default
  dur 1440 pinned (`_DEFAULT_DUR["crash"]`). **No `generators.py` change needed.**
- `pipeline/stubs.py`: delete `transitions` (line 45) and `humanize` (line 50) + the now-unused `Tempo`
  import; keep `TrackSound`/`sound_design`/`_flavor`/`_PITCHED_ROLES`. Remove the
  `if track_id == "crash": continue` guard (lines 85-86) once the stub kits carry a `crash` timbre.
- Fixtures are regenerated by `tests/_regen_milestone_fixtures.py` (never hand-edit fixture JSON).
- The stage-6 25-seed property matrix already exists (`tests/test_transitions_determinism.py:238`,
  `test_stage6_property_subset`; `_SEEDS`/`_LENGTHS=[None,180,240]` at lines 192-193) — T3 reuses that
  enumeration but drives the **fully-wired `generate_track`** for the combined-stage checks.

## Task list (ordered; models per PROMPT.md policy)

### T1 — Wire real stages + crash plumbing + tempo threading (opus)

**Files:** `src/trackgen/pipeline/orchestrator.py`, `src/trackgen/pipeline/serialize.py`,
`src/trackgen/pipeline/stubs.py`, `styles/pop_rock/timbres.yaml`, `styles/jazz/timbres.yaml`, and the
**unit** test files `tests/test_serialize.py`, `tests/test_orchestrator.py`.
**Implements:** PHASE_6 §6 (signature change), §3.6/§3.7 crash emission (already produced by stage 6;
here the Serializer must be able to emit the `crash` track), §10.7 (crash default), §10.8.

Changes:
1. **orchestrator:** import `transitions` from `trackgen.transitions`, `humanize` from
   `trackgen.humanize.stage`, `sound_design` from `trackgen.pipeline.stubs`. Replace lines 75-77 with
   the real calls — `transitions(phrases, sf, hp, ap, plan, pack)`; `phrases, tempo_events =
   humanize(phrases, sf, plan)` — and pass `tempo_events` to `serialize(...)`.
2. **serialize:** add a keyword-only additive param `tempo_events: list[Tempo] | None = None`; build
   `tempos=[Tempo(ticks=0, bpm=plan.tempo_bpm), *(tempo_events or [])]`. (V1: first at tick 0, sorted —
   ritard events are already absolute-tick ascending; add a light assertion/comment.) Add `crash` to
   `_STUB_MIX` (choose a sensible stub `(volumeDb, pan)` in the drum family, e.g. `(-4, 0.1)`).
3. **stubs:** delete `transitions` + `humanize` + unused `Tempo` import; remove the `crash`-skip guard
   in `sound_design`.
4. **timbres.yaml (both packs):** add a `crash` entry to the drums kit(s), trigger **midi 84**, mirroring
   the existing ride/hats row shape (a MetalSynth-style stub patch is fine — match what the pack already
   uses for cymbals). Loader must accept it (run the pack-load gate).
5. **Unit tests:** (a) `serialize` threads a non-empty `tempo_events` into `header.tempos` after the
   tick-0 base, order preserved, still V1-valid; empty/None → single base tempo (regression). (b) a phrase
   set containing a `crash` track serializes a crash `Track` with midi 84 and its `_STUB_MIX` mix.
   (c) `test_orchestrator.py` smoke: `generate_track` for both examples still returns a `TrackDocument`
   with `validate_document == []` **through the real stages**, and the jazz doc's `header.tempos` has
   **40 entries** (base + 39).

**Gate-handling for the whole-doc goldens:** wiring the real stages changes both fixtures, so
`tests/test_whole_document_goldens.py`'s structural-equality assertions will fail against the stale
fixtures. In **this commit**, mark those assertions `@pytest.mark.xfail(strict=True, reason="Phase 6
output legitimately changes; re-blessed in SESSION_12 T2 (dedicated commit)")` so the four gates stay
green. **Do not regenerate the fixtures in T1** — that is T2's dedicated re-bless. (The §9.4 anchor
cross-checks in that file that still hold need not be xfail'd — xfail only the assertions that depend on
the changed output; the implementer decides per-assertion and reports which were marked.)

**Verify:** four gates green (`uv run pytest` · `ruff check .` · `ruff format --check .` · `mypy`).
**Report:** the diff summary, the exact list of xfail'd assertions (for T2 to un-mark), and confirmation
that all stage-6/stage-7/Phase-1–5 goldens still pass.

### T2 — Re-bless both whole-document goldens (dedicated commit; opus, independent-arbiter posture)

**Files:** `fixtures/pop_rock.milestone.trackdoc.json`, `fixtures/jazz.milestone.trackdoc.json`,
`tests/test_whole_document_goldens.py`, `tests/_regen_milestone_fixtures.py` (only if a helper tweak is
needed). **Disjoint from T1's code and from T3's new test.**
**Implements:** ROADMAP §3 rule 3 (bless-in-spirit), PHASE_6 §7.3 milestone facts, §9.4 anchors.

1. Regenerate both fixtures through the fully-wired pipeline via `tests/_regen_milestone_fixtures.py`
   (never hand-edit). Remove the T1 xfail markers.
2. **Verify the regenerated docs (arbiter posture — the fixture is engine-blessed, but the printed facts
   must hold; on divergence escalate, do NOT tune):**
   - Every V-rule V1–V8 passes (`validate_document == []`) for both docs.
   - Jazz `header.tempos` = **40 entries**: base `{0, 69}` then the 39 §7.2 ritard events, first
     `{+240→ tick 115440, 68.5}`, last `{+7560→ tick 122760, 45.5}`; monotone-decreasing bpm; none past
     the final downbeat. (Compute absolute ticks: tag starts at 115200.) Pop `header.tempos` = **1 entry**
     (cold close, no ritard).
   - The §7.3 structural facts: fills/crashes present, HOLD extends finals (+0.05, tag dropped at
     serialize), C5 ceiling intact (no non-drum `midi > 71`), crash suppression for postchorus/breakdown
     (none in these two forms — note as N/A).
   - The §9.4 worked-example anchors still resolve (they may now carry humanized `ticks`/`velocity` — the
     anchor assertions that pin pre-humanization positions must be updated to the humanized values or
     re-expressed against structure; report any that changed and why).
   - **No bar loops byte-identical** across the doc (the §11.10 diff property — assert at least the
     mutation/jitter makes repeated sections differ).
3. Update `test_whole_document_goldens.py` so a fresh `generate_track` re-serializes structure-identically
   to each re-blessed fixture (the standing regression contract), plus the tempo-map + V-rule assertions.

**Escalation:** if any regenerated value contradicts a PHASE_6 §7.3 / §7.2 printed fact, **stop and
escalate with the divergence** (arbitration rule 1/2) — do not tune code or fixture to a printed number.
**Verify:** four gates green. **Report:** the two fixtures' high-level diffs vs. the Phase-5 versions
(tracks/notes/tempo-map deltas), the tempo-map verification, and any anchor updates.

### T3 — Whole-phase property matrix (DoD 9; opus) — parallel with T2

**Files:** a new test (`tests/test_phase6_property.py`, or a new function in
`tests/test_transitions_determinism.py` — must **not** edit T2's fixtures or `test_whole_document_goldens`).
**Implements:** PHASE_6 §11.9.

Drive the **fully-wired `generate_track`** (both real stages) over both packs × every
`pack.interpreter.supported_moods` × lengths `[None, 180, 240]` × the 25 `_SEEDS` (reuse
`test_transitions_determinism._SEEDS`). For every produced `TrackDocument`, assert §11.9:
- Fills appear only in legal fill bars (section fill-bar or included phrase-boundary fill bar).
- No drum groove event survives inside a rendered fill window.
- Crash suppression honored for `postchorus`/`breakdown` entries (no entry crash there).
- No note before tick 0 or past song end (V8-adjacent, at the phrase level pre-serialize and doc level).
- **Non-drum `midi` untouched by both stages** (C5 ceiling; no non-drum note re-pitched or `> 71`).
- Backbeat-class snare events (velocity ≥ 0.7 at back2/back4) never removed or moved by mutation.
- Every document passes **V1–V8** (`validate_document == []`).

**Escalation-watch confirmations (report explicitly):**
- **P1 latent** (§3.2 places section-boundary fill/crash unconditionally by entered type) — confirm no
  mood/length/seed combo injects a fill/crash where `drums` is inactive. If one trips it, that is a §3.2
  amendment (sign-off), **not** a silent fix — escalate.
- **C-10** (thin Serializer, no coincident same-voice drum de-dup → latent V3) — confirm no combo produces
  a V3 violation now that stage 6 adds crash+kick at shared ticks (the double-hit guard should prevent it).

**Verify:** four gates green (this adds ~ (2 packs × moods × 3 lengths × 25 seeds) documents — run pytest
with an extended timeout). **Report:** the matrix size actually exercised, pass/fail counts, and the two
escalation-watch confirmations (or escalations).

### T4 — Milestone regeneration + audition prep (DoD 10; opus, light) — after T2

**Files:** none new in `src/`; produces the playground-loadable artifacts and the checklist. The milestone
docs **are** the T2 re-blessed fixtures — this task confirms they load/play and assembles the audition.
**Implements:** PHASE_6 §11.10.

- Confirm both re-blessed fixtures load in the Phase-1 playground (`playground/`) — the same path PHASE_5
  §9.5 used. Produce/point at the two JSON docs for audition.
- Assemble the §11.10 listening checklist for the user: (1) fills lead every section change onto an audible
  crash; (2) the jazz ritard reads as a band slowing, not steps; (3) the pop ending rings and releases
  together; (4) no bar loops byte-identically; (5) the swing feel survives the ritard.
- This is a **USER AUDITION GATE** (like PHASE_5 §9.5) — the orchestrator presents the artifacts + checklist
  and waits for the user's confirmation. DoD 10 is marked PROVEN only on user sign-off; if the user defers,
  log it as pending (as PHASE_1 §9.6 remains) and do not block phase close-out on it beyond noting it.

**Report:** the audition artifacts (paths), the checklist, and the DoD-10 status (proven / pending user).

### T5 — Whole-phase review + full DoD 1–11 + §10 amendment audit + close-out (orchestrator; opus review agents)

Per PROMPT.md step 3/4, **across all three chunks** (not per-task diffs):

1. Dispatch **fresh opus** review agents, separate lenses in parallel over the whole Phase-6
   implementation (`transitions/`, `humanize/`, the C3 wiring, packs `transitions.yaml`/`feel.yaml`/crash
   timbres): (a) correctness/logic bugs; (b) contract compliance vs. PHASE_6 §3/§5/§6 + frozen upstream
   contracts; (c) test quality + DoD coverage; (d) code quality/simplification. Consider the C-11
   provenance-tag mechanism and the C-10/P1 latents in scope.
2. For each finding: dispatch a **validation** agent to confirm before any change; confirmed → fix agent +
   gate re-run (2-cycle bound).
3. **Full §11 DoD 1–11 checklist** with evidence (test names, fixture paths, command output), spanning all
   chunks (1: loader; 2: feel; 3: device narratives; 4: rendering; 5: humanizer; 6: ritard; 7: determinism;
   8: synthetic fixtures; 9: property matrix — T3; 10: milestone — T4; 11: amendments — below).
4. **DoD 11 / §10 amendment audit** — verify every §10 amendment is present and consistent in the actual
   docs: ROADMAP §3 pipeline diagram (mutation on stage 6, "2-bar units") + §2 + §4; PHASE_1 §4 recap
   (`Phrase[] + tempo events`), §4.5 tag vocab (`fill`/`crash`/`var`/`hold`), §7 Q5 resolved; PHASE_5 §3.2
   PT12, §8.2 crash row, §8.1/§8.3 stub replacement + `tempoEvents`; PHASE_2 §7.2 `dynamicsRange` row. Log a
   CAVEAT for any missing/inconsistent amendment.
5. Close out: PROGRESS.md (statuses, session-12 log row, fresh handoff → **Phase 7**), CAVEATS entries,
   final commit. Report to user.

## Model assignments

T1, T2, T3 → **opus** (real judgment: wiring correctness, arbiter re-bless, property invariants). T4 →
**opus** (light; audition prep + playground confirmation). T5 review/validation/fix agents → **opus**.
No Fable, no Sonnet (nothing here clears the "truly trivial" bar). Every `Agent` dispatch sets `model`
explicitly.

## Ordering / parallelism

**T1 → then (T2 ‖ T3) → T4 → T5.** T2 (fixtures + `test_whole_document_goldens.py`) and T3 (new property
test) touch disjoint files and both depend only on T1's wiring, so they run in parallel. T4 depends on
T2's re-blessed fixtures. T5 is last.

## Per-task verification (all tasks)

Four gates, run and read every time: `uv run pytest` (extended timeout — full suite ~4 min, 2734+ tests) ·
`uv run ruff check .` · `uv run ruff format --check .` · `uv run mypy`. Commit at the verified gate; update
PROGRESS.md immediately with the commit hash.
